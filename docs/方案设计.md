## **1. 架构总览与核心分流总线**

当用户点击系统分享并激活 Share Extension 时，插件的 ShareViewController 在 viewDidLoad 的前 50 毫秒内，通过**宿主特征嗅探**与**文件物理尾部二进制扫描**，将逻辑划分为 **2大物理前置特征** 与 **4大精简业务通路**：

### **1.1 宿主特征嗅探机制**

通过检测文件来源路径中是否包含 `wps_share_temp_dir` 字符串来判定宿主类型：

- **WPS 宿主判定**：路径包含 `wps_share_temp_dir`（WPS 分享临时目录特征）
- **外部宿主判定**：路径不包含该特征（微信、QQ、文件App等）

**典型 WPS 分享路径示例**：
```
/private/var/mobile/containers/data/application/xxx/tmp/wps_share_temp_dir/xxx/文件名.docx
```

 

<br />

```
                               收到传入的文件 tempFile
                                         |
                       +-----------------+-----------------+
                       |                                   |
            [ 检测到：尾部存在元数据 ]           [ 检测到：尾部无元数据 ]
                       |                                   |
           +-----------+-----------+           +-----------+-----------+
           |                       |           |                       |
     ( 宿主为外部/保险箱 )    ( 宿主为 WPS )   ( 宿主为外部/微信 )   ( 宿主为 WPS )
           |                       |           |                       |
     【通路 1：统一看密】     【通路 2：纯看未改】 【通路 3：野生文件】  【通路 4：正常写回】

```

<br />

 

## **2. 4 大精简业务通路完整路径规范**

### **🛠️ 通路 1：统一查看密码线（核心记账防线 · 原组合 A & E 合并）**

● **适用场景**：用户在微信/QQ 中收到带有元数据的文件（读场景① & ②），或者在系统“文件 App”的保险箱目录中直接对有元数据的文件点击分享（读场景③）。

● **用户心智**：**“我只是想用插件临时查个密码，然后我自己去手动打开它。”**

● **核心动作（物理轻量、数据有心）**：

○ **元数据提取**：扫描 tempFile 尾部，成功提取出有效的企业资产 UID。

○ **界面交互**：插件原地进入 **【统一查密画布】**（高亮、特大字号直接在屏幕中央砸出该 UID 对应的解密密码，并展示标准资产卡片）。界面只提供一个动作大按钮：**\[ 复制密码并退场 ]**。

○ **无感记账（至关重要）**：在用户点击“复制”的瞬间，插件在后台花费 1 毫秒通过 **REPLACE INTO** 语法将该文件的 \[UID : 密码句柄 : 当前时间戳] 写入 App Group 共享数据库，并显式标记 **is\_local\_vault = 0**（表明物理文件此时不在保险箱）。**这一步是为通路 4 的全自动撞击埋下绝对完美的垫脚石**。

○ **物理零动作**：**绝对不调用 copyItem 复制或搬运文件**。文件依然各自安全地躺在它们原来的地方（微信的在微信，保险箱的在保险箱）。

○ **智能退场与回弹**：内存级将密码挂载到系统剪贴板（60秒销毁）后，插件瞬间主动调用 completeRequest 自动闪退。用户瞬间视觉无缝回弹到原宿主应用（微信或文件 App），直接双击文件拉起 WPS 粘贴即可。

### **🛠️ 通路 2：WPS 纯看未改写回线（组合 8）**

● **适用场景**：用户在 WPS 内打开了一个带有元数据的文件，但**纯阅读、未做任何内容编辑修改**，随后直接在 WPS 内点击了分享发回插件。

● **物理特征**：由于 WPS 没有触发真正的重写保存，文件的 ZIP 结构未被破坏，**尾部元数据奇迹般地保留了下来**。

● **核心动作**：

○ **闪电免撞击**：判定宿主来自于 WPS，且扫描发现尾部存在有效元数据。立即**免去耗时的后台密码撞击**，直接提取尾部 UID。

○ **账本反查**：用该 UID 去 SQLite 数据库反查出对应的老资产记录。

○ **精准直通**：界面跳过所有密码拦截，直接原地全屏切换为 **【UI 状态 B：精准同步确认画布】**。提示用户确认覆盖或另存。

### **🛠️ 通路 3：野生资产首次实名登记线（组合 1 & 3）**

● **适用场景**：用户从外部微信、QQ 或 iCloud 目录调入了一个纯粹野生、游离在企业生态之外的、没有任何元数据标记的加密文件。

● **用户心智**：**“把外部野生的加密资产正式收纳并登记落户到企业保险箱中。”**

● **核心动作**：

○ **乱码纠偏**：如果文件名由于微信缓存机制变为乱码（如 \~6fa8b2a3c9），触发 **【真名确认与纠偏画布】（UI 状态 A）**。流式读取前 4 字节魔数 50 4B 03 04 确保为合规 OOXML 格式，浅扫描 ZIP 目录自动关联补齐后缀。

○ **防黑户拦截（黄色画布）**：基于“野生文件不瞎认”原则，立即弹出 **【黄色画布（登记新文件）】**，强制要求用户手动输入该文档在 WPS 内设置的正确密码。

○ **解密合法性验证**：调用 OfficeCryptoVerifier 在后台异步线程校验用户输入的密码能否真正解开 tempFile。

■ *校验失败*：原地平滑降级切换至错误画布，阻断流程。

■ *校验通过*：确认为合法落户资产。在内存中生成全局唯一的 UID\_全新，连同有效密码**追加写入文件尾部**。

○ **首次入库落盘**：将新生成的 UID 映射关系写入数据库（标记 **is\_local\_vault = 1**）。最后采用 NSFileCoordinator 协调的同步 moveItem 操作，将文件安全转正落盘到共享沙盒的 **“安全保险箱”目录** 中，并设置备份排除标记。

### **🛠️ 通路 4：WPS 正常编辑写回线（核心回写流 · 组合 7）**

● **适用场景**：用户在手机 WPS 里面大刀阔斧地修改了原本属于生态内的合规文件，点击保存并分享回插件。

● **物理特征**：由于 WPS 重新编译并打包了 ZIP 压缩流，**文件尾部原本的元数据已经被 WPS 彻底抹空（无元数据状态）**。

● **核心动作**：

○ **后台全自动降维打击（解密撞击）**：**严禁直接弹窗逼用户选**。插件在后台线程（DispatchQueue.global）中，从 SQLite 账本中按时间戳倒序取出 **前 5 条活跃记录的密码队列**。

○ **撞击结果二元分流处理**：

■ **情况 A（撞击成功 ·“正规军洗澡回来了”）**：

● 由于**通路 1** 已经在用户查密时悄悄执行了“无感记账”，此时密码队列中**必然 100% 包含并命中**该文件的解密密码！

● 代码通过撞中的密码，瞬间反查出对应的 UID\_老 和原始 file\_name。

● **零交互直通**：直接跳过强制密码输入，原地全屏切换为 **【UI 状态 B：精准同步确认画布】**。

● 用户点击 **\[确定同步覆盖]** ➔ 调用 NSFileCoordinator 内核锁重试逻辑，删除保险箱老文件，将回传文件更名覆盖转正，并将数据库中的 is\_local\_vault 状态从 0 修正为 1。**保持原 UID 基因在云端和本地绝对不变**。

■ **情况 B（所有密码均撞击失败 · 用户在 WPS 里顺手改了密码，或完全手创新资产）**：

● 此时技术手段彻底失效，正式降级切入 **【黄色画布（密码强制捕获与外部资产归宿确认）】**。

● 用户在画布中输入最新密码，验证通过后，全屏提供两个大按钮供用户肉眼决策：

○ **\[ 登记为本地新文件 ]**：生成全新的 UID\_全新 追加写入尾部并落盘。

○ **\[ 关联并覆盖本地已有文件 ]**：平滑切换至 **【状态 D：基于 SwiftUI 惰性高性能滑动认领画布】**，由用户人工指定要覆盖替换哪一个老资产，指定后注入老 UID 并跨卷覆盖。

## **3. 数据库结构与多进程全自治维护规范**

### **3.1 核心轻量账本表结构 (file\_mapping\_table)**

SQL

<br />

```
CREATE TABLE IF NOT EXISTS file_mapping_table (
    uid TEXT PRIMARY KEY,                 -- 文件的唯一加密基因 UID (主键)
    file_name TEXT NOT NULL,              -- 标准化小写文件名 (用于辅助模糊匹配)
    password_hash TEXT NOT NULL,          -- 经托管的密码密文句柄/解密盐值
    last_access_time INTEGER NOT NULL,    -- 最后一次读取/写入的时间戳 (用于 LRU 淘汰和Top 5排序)
    file_size INTEGER NOT NULL,           -- 物理文件大小 (用于辅助特征判定)
    is_local_vault INTEGER DEFAULT 0      -- 核心标记：0 表示文件在外部(如微信)纯记账；1 表示文件已落盘在保险箱
);
CREATE INDEX IF NOT EXISTS idx_filename_size ON file_mapping_table(file_name, file_size);
CREATE INDEX IF NOT EXISTS idx_access_time ON file_mapping_table(last_access_time);

```

<br />

 

### **3.2 刚性双向保洁总线（谁来删，怎么删）**

由于 Share Extension 插件进程用完即被系统销毁，**重度事务的大扫除维护全权交由主 App 在后台默默执行**：

1.  **僵尸索引自动清洗（主 App 异步执行）**： 主 App 每次启动或由后台唤醒时，利用 FileManager 异步扫描一遍公共沙盒（安全保险箱）目录。如果发现数据库中某条记录 **is\_local\_vault = 1**，但在公共目录下已经找不到它的物理实体了（说明用户直接在系统的“文件 App”里把文件长按删除了）。主 App 立即执行批量删除，剔除僵尸索引：

SQL

<br />

```
DELETE FROM file_mapping_table WHERE uid = ?;

```

<br />

 

2.  **磁盘空间 LRU 硬性清理（主 App 限额控制）**： 当共享保险箱目录总占用超过硬性阈值（如 1GB）时，主 App 计算超额空间，从数据库中按照 last\_access\_time 升序（最久没人访问的文件）查询出记录：

SQL

<br />

```
SELECT uid, file_name FROM file_mapping_table WHERE is_local_vault = 1 ORDER BY last_access_time ASC;

```

<br />

 

主 App 循环删除这批最冷门文件的本地物理实体，并同步执行数据库 DELETE，直到磁盘空间回落到安全线内。

### **3.3 多进程 WAL 并发硬化控制**

为了绝对防止主 App 和 插件在同一毫秒读写数据库冲突抛出 SQLITE\_BUSY 错误，底层连接强制注入以下两条刚性约束：

● **开启 WAL 模式**：初始化 SQLite 链接时，强制执行 PRAGMA journal\_mode=WAL;。

● **配置 2 秒 Busy Timeout 避让重试锁**：

Swift

<br />

```
// Swift 底层连接硬化：如果遇到多进程锁冲突，最长自动等待重试 2000 毫秒，2秒内有空隙就无感插队写入
sqlite3_busy_timeout(dbConnection, 2000)

```

<br />

 

 

## **4. Office 密码验证技术实现**

### **4.1 支持的加密格式**

当前实现支持 **OLE2 复合文档格式** 的 **Standard Encryption (version 4.2)**，这是 Office 2007+ 使用的加密标准。

### **4.2 OLE2 复合文档解析**

实现了完整的 OLE2 复合文档解析器（OLE2Parser），包含以下核心功能：

- **文件头解析**: 读取扇区大小、迷你扇区大小、FAT 扇区数、根目录扇区等关键参数
- **DIFAT 读取**: 支持扩展 DIFAT 链读取，处理大文件场景
- **FAT 读取**: 从 DIFAT 条目读取完整的 FAT 链
- **目录项遍历**: 解析目录项结构（名称、类型、起始扇区、大小），支持存储/流/根目录类型
- **流读取**: 支持普通流和迷你流读取

### **4.3 EncryptionInfo 解析**

从 OLE2 文档中提取 `EncryptionInfo` 流，解析以下关键字段：

| 字段 | 说明 |
|------|------|
| versionMajor/minor | 加密版本（4.2 表示 Standard Encryption） |
| headerSize | Header 结构大小 |
| algId | 加密算法 ID（0x660E = AES-128） |
| algIdHash | 哈希算法 ID（0x8004 = SHA-1） |
| keySize | 密钥大小（128 位） |
| salt | 盐值（16 字节） |
| encryptedVerifier | 加密的验证值（16 字节） |
| encryptedVerifierHash | 加密的验证哈希值（32 字节） |

### **4.4 密钥派生算法（ECMA-376 标准）**

严格按照 ECMA-376 标准实现密钥派生：

```
1. 初始哈希: H0 = SHA-1(salt + password_UTF16LE)

2. 迭代哈希: H(i) = SHA-1(iterator(i) + H(i-1))，共迭代 50000 次
   - iterator(i): 4 字节小端序表示的迭代次数

3. 最终哈希: finalHash = SHA-1(H50000 + blockKey)，blockKey = [0,0,0,0]

4. fillAndXor 操作:
   - x1 = SHA-1(0x36填充64字节 XOR finalHash)
   - x2 = SHA-1(0x5c填充64字节 XOR finalHash)

5. 最终密钥: AES_KEY = (x1 + x2).prefix(keySize)
```

### **4.5 密码验证流程**

```
输入密码 → 密钥派生 → AES-ECB解密
                              │
              ┌───────────────┴───────────────┐
              ▼                               ▼
    encryptedVerifier                encryptedVerifierHash
              │                               │
              ▼                               ▼
         verifier                      decryptedVerifierHash
              │                               │
              ▼                               │
    SHA-1(verifier)                           │
              │                               │
              ▼                               ▼
      computedHash                   decryptedVerifierHash[0..19]
              │                               │
              └───────────────┬───────────────┘
                              ▼
                        比对结果 → true/false
```

## **5. 画布交互**

 

**画布颜色**

**核心触发场景**

**台前交互组件**

**刚性动作按钮配置 (Action Buttons)**

**点击后的底层归宿与退场机制**

**🟢 绿色**

**通路 1**：外部微信流入或保险箱文件且**有元数据**。

1\. 资产身份卡片

 

2\. 密码大字明文

① **\[ 复制密码并退场 ]**

 

② **\[ 取消 ]**

内存级记账 REPLACE INTO，密码上系统剪贴板（60s销毁），插件自动闪退 completeRequest。

**🟠 橙色**

**通路 3**：外部微信流入且**无元数据**（纯野生文件）。

1\. 密码捕捉输入框

 

2\. 乱码真名纠偏框

① **\[ 登记为本地新文件 ]**

 

② **\[ 覆盖其他文件 ] (补齐)**

 

③ **\[ 取消 ]**

输入密码经 OfficeCryptoVerifier 验证通过后推进。① 产生新 UID 并深拷贝落盘保险箱；② 唤起状态 D 惰性滑动列表。

🔵 蓝色

 
(子态 A)

**通路 2/4**：来自 WPS 导出，且**后台密码撞击成功**（或带元数据直通）。

1\. 资产回流卡片

 

2\. 匹配提示（区分文件/记录）：根据数据库记录的 `is_local_vault` 字段区分显示
   - `is_local_vault = 1`：显示"匹配到文件：xxx"
   - `is_local_vault = 0`：显示"匹配到记录：xxx"

① **\[ 确定同步覆盖 ]**：校验临时文件密码，失败则弹窗输入密码

② **\[ 另存为新文件 ]**：校验密码后弹出文件名弹窗，使用文件自身UID（无则新生成）

③ **\[ 覆盖其他文件 ]**：校验密码后唤起状态 D 列表

④ **\[ 取消 ]**

① 保持原 UID 基因不变，利用 NSFileCoordinator 跨卷物理覆盖保险箱老实体，自动退场。③ 唤起状态 D 列表。

🔵 蓝色

 
(子态 B)

**通路 4 降级**：来自 WPS 导出，但用户改了密码或手创新文件导致**撞击失败**。

**通路 2 降级**：来自 WPS 导出，文件尾部有元数据但数据库无对应记录。

1\. 强制最新密码输入框

 

2\. 模糊原名提示

① **\[ 另存为新文件 ]**：校验密码后弹出文件名弹窗，使用文件自身UID（无则新生成）

② **\[ 覆盖其他文件 ]**：校验密码后唤起状态 D 列表

③ **\[ 取消 ]**

手动输入的新密码通过验证后激活按钮。① 产生新 UID（原文件有则保留）并深拷贝落盘保险箱；② 唤起状态 D 列表。

**⚪ 状态 D**

**统一全屏视图**：由上述通路的手动关联覆盖触发。

1. **ScrollView + LazyVStack**

 

2\. 每页 20 行分页加载

① **\[ 任意列表行卡片 ]**（点击即选中）

 

② **\[ 返回上一步 ]**

用户点击列表中的某一个老资产行后，直接强行将该老资产的 UID 基因注入到当前文件中，执行标准的防同名冲突覆盖转正并退场。

## **6. 资产保险箱界面设计**

### **6.1 界面架构**

主应用的资产保险箱界面基于 SwiftUI 实现，采用 `NavigationStack` + `List` 结构，提供完整的资产管理功能。界面移除导航栏标题，新增顶部搜索框。

```
┌─────────────────────────────────────────────────────────┐
│                    SearchBar                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │  🔍 搜索文件名...                        🔄 刷新  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│                    List (AssetRow)                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │  📄 文件名.docx                    2.3 MB 🔒      │  │
│  │  2026-06-17 14:30                                  │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  📊 表格.xlsx                     1.1 MB 🔒      │  │
│  │  2026-06-16 10:20                                  │  │
│  ├──────────────────────────────────────────────────┤  │
│  │  📈 演示.pptx                     5.6 MB 🔓      │  │
│  │  2026-06-15 09:15                                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │              暂无资产记录（空状态）                 │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### **6.2 核心组件**

| 组件 | 功能 | 关键属性 |
|------|------|----------|
| **AssetRow** | 文件列表行 | 文件图标、文件名、大小、时间、保险箱状态 |
| **AssetDetailView** | 文件详情页 | 完整文件信息、密码展示与复制 |
| **FileIconView** | 文件类型图标 | 根据扩展名显示不同图标和颜色 |
| **InfoRow** | 信息展示行 | 标签+值的水平布局 |
| **SearchBar** | 顶部搜索框 | 实时模糊搜索，清除按钮 |

### **6.3 搜索索引机制**

为了实现高效的模糊搜索，采用预构建倒排索引的策略：

**索引构建流程:**
```
主页打开/刷新 → loadRecords() → SearchIndex.buildIndex() → 字符级n-gram分词 → 构建倒排索引
```

**索引结构:**
```
索引字典: { "关键字符": [匹配记录数组] }
示例: { "文档": [record1, record2], "密码": [record3], ... }
```

**分词策略:**
- 字符级n-gram：单字 + 双字组合
- 示例："密码.txt" → ["密","码","密m","密m码","码.", "码.t", ...]

**搜索流程:**
```
用户输入搜索词 → tokenize() 分词 → 倒排索引查询 → 交集过滤 → 评分排序 → 返回结果
```

**评分策略:**
- 前缀匹配：+100分
- 完全匹配：+200分
- 字符匹配数：+10分/个

### **6.4 功能流程**

**文件列表加载:**
```
onAppear → loadRecords() → queryAllLocalVaultRecords() → 构建搜索索引 → sorted(by: last_access_time DESC) → 更新UI
```

**模糊搜索流程:**
```
用户输入 → filterRecords() → SearchIndex.search() → 倒排索引查询 → 评分排序 → 更新filteredRecords
```

**文件预览与自动复制密码:**
```
点击列表行 → openFile() → 异步读取文件尾部密码 → 复制到剪贴板(30秒后自动清理) → 显示Toast提示 → 复制文件到系统临时目录 → 优先UIApplication.shared.open()唤起第三方应用(如WPS) → 失败时降级UIDocumentInteractionController系统预览 → 预览关闭后自动清理临时文件 → 更新数据库last_access_time → 用户在密码输入框粘贴使用
```

**详情查看与密码复制:**
```
点击锁图标 → 打开 AssetDetailView → readPassword() 异步读取 → 显示密码 → 点击复制按钮 → UIPasteboard.general.string = password
```

**文件删除:**
```
左滑列表行 → 点击删除 → deleteRecord() → 删除物理文件 → 删除数据库记录 → 更新列表
```

### **6.5 文件打开策略**

采用优先级降级策略，确保最佳用户体验：

```
┌─────────────────────────────────────────────┐
│         1. 优先唤起外部应用                   │
│   UIApplication.shared.open() → 系统选择WPS   │
│                  ↓ 失败                      │
│         2. 降级系统预览                       │
│   UIDocumentInteractionController.presentPreview() │
└─────────────────────────────────────────────┘
```

**DocumentInteractionManager职责:**
- 持有UIDocumentInteractionController和delegate的强引用
- 管理预览控制器的生命周期
- 预览关闭后自动清理资源

**降级原因:**
- `UIApplication.shared.open()`：优先使用用户已安装的专业应用（如WPS），提供完整编辑功能
- `UIDocumentInteractionController`：系统预览，支持加密Office文件的密码输入框；对于未安装WPS的用户提供兜底预览能力

## **7. 重名检测与重命名机制**

### **7.1 设计目标**

防止保险箱目录中出现同名文件导致覆盖冲突，确保每个文件拥有唯一的文件名。

### **7.2 检测流程**

```
用户点击"登记为本地新文件"
         │
         ▼
┌───────────────────────────┐
│ fileNameExistsInVault()   │ ← 检查保险箱目录是否存在同名文件
└───────────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
 存在重名    无重名
    │         │
    ▼         ▼
弹出重命名    直接注册
对话框        performRegisterNewFile()
    │
    ▼
用户输入新文件名
    │
    ▼
confirmRename() → performRegisterNewFile()
```

### **7.3 重命名对话框设计**

| 元素 | 说明 |
|------|------|
| 标题 | "发现重名文件" |
| 提示文字 | "保险箱中已存在同名文件，请输入新名称" |
| 输入框 | 默认填充原文件名，允许用户修改 |
| 确定按钮 | 验证文件名非空后执行注册 |
| 取消按钮 | 关闭对话框，不执行任何操作 |

### **7.4 关键代码逻辑**

```swift
// 检测重名
private func fileNameExistsInVault(fileName: String) -> Bool {
    let vaultDir = containerURL.appendingPathComponent(safeVaultDir)
    let destURL = vaultDir.appendingPathComponent(fileName)
    return FileManager.default.fileExists(atPath: destURL.path)
}

// 注册新文件时的重名检查
private func registerNewFile(password: String) {
    if fileNameExistsInVault(fileName: detectedFileName) {
        newFileNameInput = detectedFileName  // 默认填充原文件名
        showRenameDialog = true
        return
    }
    performRegisterNewFile(password: password, fileName: detectedFileName)
}
```

## **8. 关联覆盖密码验证机制**

### **8.1 设计目标**

在用户选择"关联并覆盖本地已有文件"之前，强制验证用户输入的密码是否正确，防止误操作导致文件覆盖失败或数据损坏。

### **8.2 验证流程**

```
用户在黄色画布输入密码
         │
         ▼
用户点击"覆盖其他文件"
         │
         ▼
┌─────────────────────────────┐
│ verifyAndShowAssetSelection │ ← 触发密码验证流程
└─────────────────────────────┘
         │
         ▼
┌─────────────────────────────┐
│ OfficeCryptoVerifier验证    │ ← 异步验证密码正确性
└─────────────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
 验证通过    验证失败
    │         │
    ▼         ▼
showAssetSelection()  显示错误提示
→ 状态D               verificationError = "密码验证失败"
```

### **8.3 验证时机**

| 场景 | 是否需要验证 | 说明 |
|------|-------------|------|
| 黄色画布（通路3）点击"覆盖其他文件" | ✅ 是 | 需要验证用户输入的密码 |
| 蓝色画布B点击"覆盖其他文件" | ✅ 是 | 需要验证用户输入的密码 |
| 蓝色画布A点击"覆盖其他文件" | ✅ 是 | 需要验证撞库获得的密码，失败则弹窗输入 |

### **8.4 安全性保障**

1. **防误操作**：用户必须输入正确密码才能进入资产选择列表，防止选择错误的资产进行覆盖
2. **密码复用**：验证通过的密码会被复用为覆盖操作的密码，确保覆盖后文件可正确解密
3. **错误反馈**：验证失败时显示明确的错误提示，引导用户重新输入

## **9. UID优先从文件尾部读取机制**

### **9.1 设计目标**

确保关联覆盖操作能够正确继承被覆盖文件的身份标识（UID），优先从文件物理尾部读取，保证数据的准确性和可靠性。

### **9.2 UID获取优先级**

```
关联覆盖时获取UID的优先级：
1. 从被覆盖文件B的尾部读取WPPM UID标记（最高优先级）
2. 从数据库查询文件B的记录获取UID（降级方案）
```

### **9.3 设计理由**

| 优先级 | 方式 | 理由 |
|--------|------|------|
| 1 | 文件尾部读取 | 文件物理存储的UID是最权威的来源，不会因数据库同步延迟或数据损坏而失效 |
| 2 | 数据库查询 | 作为降级方案，当文件尾部标记损坏或丢失时仍能获取UID |

### **9.4 执行流程**

```
用户选择用文件A覆盖文件B
         │
         ▼
┌─────────────────────────────┐
│ 读取文件B尾部的UID标记       │ ← ZipExtraFieldManager.shared.readUid(from:)
└─────────────────────────────┘
         │
    ┌────┴────┐
    ▼         ▼
 读取成功    读取失败
    │         │
    ▼         ▼
 使用文件B    从数据库查询
 的UID        文件B的UID
    │
    ▼
将B的UID写入A文件尾部
    │
    ▼
将A移动覆盖到B的位置
    │
    ▼
更新数据库记录
```

## **10. 版本 2.8.0 更新记录（2026-06-21）**

### **10.1 Office密码验证方式精简**

**问题**：OfficeCryptoVerifier中包含多种未使用或冗余的加密验证方式和辅助方法，导致代码臃肿。

**解决方案**：仅保留以下两种有效验证方式：

| 验证方式 | 说明 |
|----------|------|
| **Agile Encryption 4.4** | Office 2007+ 使用的 ECMA-376 标准加密，通过 XML 格式解析验证 |
| **Standard Encryption** | 旧版 Office 使用的二进制格式加密，支持版本 2.2/3.2/4.2 |

**清理的代码**：
- `verifyAgileBinary` - 重复的二进制解析方法
- `extractBase64TagValue`、`extractBase64Value`、`extractAttributeValue` - 未使用的冗余 XML 提取方法
- `pbkdf2` - 不再使用（已替换为 ECMA-376 自定义迭代哈希）
- `hmac` - 不再使用

### **10.2 状态D返回导航修复**

**问题**：当从蓝色画布（`blueCanvasA` 或 `blueCanvasB`）点击"关联并覆盖本地已有文件"进入状态D后，点击"返回上一步"按钮时，`goBack()` 函数硬编码返回 `.yellowCanvas`，导致错误地回到了橙色画布。

**解决方案**：

1. **添加状态变量**：新增 `previousState` 状态变量，用于记录进入状态D之前的状态：
   ```swift
   @State private var previousState: ActionState = .yellowCanvas
   ```

2. **保存当前状态**：修改 `showAssetSelection()` 函数，在切换到状态D之前保存当前状态：
   ```swift
   private func showAssetSelection() {
       previousState = actionState  // 保存当前状态
       assetList = AppGroupDBManager.shared.queryAllLocalVaultRecords()
       actionState = .stateD
   }
   ```

3. **返回之前状态**：修改 `goBack()` 函数，返回之前保存的状态：
   ```swift
   private func goBack() {
       actionState = previousState  // 返回之前的状态
   }
   ```

**修复效果**：无论从黄色画布、蓝色画布A还是蓝色画布B进入状态D，点击"返回上一步"都会正确回到原来的画布。

### **10.3 蓝色画布逻辑优化与Bug修复**

**问题**：蓝色画布B（通路4撞击失败/通路2数据库无记录）缺少"另存为新文件"按钮，且"另存为新文件"功能使用数据库匹配记录的UID导致覆盖原有文件。

**解决方案**：

1. **蓝色画布A优化**：
   - 按钮文案调整："选择其他文件关联覆盖" → "覆盖其他文件"
   - 添加密码验证逻辑：所有按钮操作前先验证密码，失败则弹窗输入
   - 匹配提示区分：根据 `is_local_vault` 字段显示"匹配到文件：xxx"或"匹配到记录：xxx"
   - 另存为新文件：使用文件自身UID（无则新生成），避免覆盖原有记录

2. **蓝色画布B重构**：
   - 移除"确定同步覆盖(同名老资产)"和"确认登记并保留身份"按钮
   - 新增"另存为新文件"按钮，与蓝色画布A统一
   - 按钮文案调整："关联并覆盖本地已有文件" → "覆盖其他文件"
   - 添加密码验证：点击按钮前先验证密码，失败则阻止操作

3. **新增组件**：
   - `PendingAction` 枚举：定义待执行操作类型（同步覆盖、另存为、关联覆盖）
   - `passwordInputDialogView`：密码验证失败时的弹窗组件
   - `confirmPasswordInput()`：密码输入弹窗确认逻辑
   - `executePendingAction()`：根据待执行操作类型执行对应操作

**修复效果**：蓝色画布A和B操作流程统一，密码验证逻辑完善，另存为新文件不会覆盖原有记录。

## **11. 版本 3.0.0 更新记录（2026-06-22）**

### **11.1 主页搜索功能优化**

**问题**：随着资产保险箱中的文件增多，用户难以快速找到目标文件。

**解决方案**：

1. **移除导航栏标题**：
   - 删除 `.navigationTitle("资产保险箱")`
   - 导航栏只保留刷新按钮

2. **新增顶部搜索框**：
   - 搜索框位于列表顶部，使用独立背景区域
   - 支持实时模糊搜索文件名
   - 搜索时忽略大小写，使用 `localizedCaseInsensitiveContains` 实现中文友好匹配

3. **预构建搜索索引**：
   - 创建 `SearchIndex` 类，在主页打开或刷新时构建倒排索引
   - 采用字符级 n-gram 分词（单字 + 双字组合）
   - 搜索时直接查索引，无需遍历整个列表

**SearchIndex 核心实现**:

```swift
class SearchIndex {
    private var index: [String: [FileMappingRecord]] = [:]
    private var allRecords: [FileMappingRecord] = []
    
    // 构建索引
    func buildIndex(records: [FileMappingRecord]) {
        allRecords = records
        index.removeAll()
        
        for record in records {
            let tokens = tokenize(text: record.file_name)
            for token in tokens {
                if index[token] == nil {
                    index[token] = []
                }
                if !index[token]!.contains(where: { $0.uid == record.uid }) {
                    index[token]!.append(record)
                }
            }
        }
    }
    
    // 搜索
    func search(query: String) -> [FileMappingRecord] {
        let queryTokens = tokenize(text: query)
        // 交集过滤 + 评分排序
        ...
    }
}
```

**评分策略**:
| 策略 | 分数 | 说明 |
|------|------|------|
| 前缀匹配 | +100 | 文件名以搜索词开头 |
| 完全匹配 | +200 | 文件名等于搜索词 |
| 字符匹配 | +10/个 | 搜索词字符在文件名中的匹配数 |

### **11.2 文件打开方式优化**

**问题**：
- 直接使用 `UIDocumentInteractionController.presentPreview()` 会显示系统预览，无法优先唤起用户已安装的 WPS
- 预览控制器和 delegate 被过早释放导致预览失败

**解决方案**：

1. **优先级降级策略**：
   - 优先使用 `UIApplication.shared.open()` 唤起外部应用
   - 失败时降级到 `UIDocumentInteractionController` 系统预览

2. **生命周期管理**：
   - 创建 `DocumentInteractionManager` 类（`@StateObject`）管理控制器生命周期
   - 持有 `UIDocumentInteractionController` 和 delegate 的强引用
   - 预览关闭后自动清理资源

**DocumentInteractionManager 核心实现**:

```swift
class DocumentInteractionManager: ObservableObject {
    @Published private(set) var isPreviewing = false
    private var docController: UIDocumentInteractionController?
    private var delegate: DocumentInteractionDelegate?
    
    func openFile(url: URL, uid: String, fileName: String, presentingViewController: UIViewController) {
        // 1. 优先唤起外部应用
        UIApplication.shared.open(url) { [weak self] success in
            if success {
                // 外部应用打开成功，清理资源
                ...
            } else {
                // 降级到系统预览
                self?.presentSystemPreview(...)
            }
        }
    }
    
    private func presentSystemPreview(...) {
        docController = UIDocumentInteractionController(url: url)
        delegate = DocumentInteractionDelegate(...)
        docController?.delegate = delegate
        docController?.presentPreview(animated: true)
    }
}
```

**降级原因**:
| 方式 | 优点 | 缺点 |
|------|------|------|
| `UIApplication.shared.open()` | 优先使用 WPS 等专业应用，提供完整编辑功能 | 需要系统能找到可处理该文件类型的应用 |
| `UIDocumentInteractionController` | 系统原生预览，支持加密 Office 文件密码输入 | 预览功能相对简单 |

**DocumentInteractionDelegate 关键方法**:
- `documentInteractionControllerViewControllerForPreview:`：提供预览的父视图控制器
- `documentInteractionControllerDidEndPreview:`：预览关闭回调
- `documentInteractionControllerDidDismissOpenInMenu:`：打开方式菜单关闭回调
 

 
